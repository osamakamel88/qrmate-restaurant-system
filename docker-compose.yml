version: '3.8'

services:
  qrmate-app:
    build: .
    container_name: qrmate-instafeed
    restart: always
    ports:
      - "3001:3001"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - PORT=3001
      - NODE_ENV=production
